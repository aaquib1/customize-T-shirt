export const metadata = {
  title: "My Drafts",
  description: "Browse and continue editing your saved t-shirt design drafts.",
  robots: {
    index: false,
    follow: true,
  },
};

export default function DraftsLayout({ children }) {
  return children;
}
