export const metadata = {
  title: "Checkout",
  description: "Enter your shipping details and pay securely via UPI.",
  robots: {
    index: false,
    follow: true,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
